package sn.oas.facturation.features.technicien.dto;

/** Corps de requête pour renseigner les pannes détectées (listeDefauts) sur un ordre assigné. */
public record PannesRequest(String listeDefauts) {}
