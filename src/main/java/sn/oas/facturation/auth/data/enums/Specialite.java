package sn.oas.facturation.auth.data.enums;

/**
 * Spécialité d'un {@link sn.oas.facturation.auth.data.entity.Technicien}.
 * Enum fermé (pas de texte libre) — voir spec technicien.
 */
public enum Specialite {
    MECANIQUE_GENERALE,
    ELECTRICITE_AUTO,
    CARROSSERIE_PEINTURE,
    TOLERIE,
    CLIMATISATION,
    DIAGNOSTIC_ELECTRONIQUE,
    PNEUMATIQUE
}
